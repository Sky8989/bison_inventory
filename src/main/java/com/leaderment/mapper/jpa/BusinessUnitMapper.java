package com.leaderment.mapper.jpa;

import com.leaderment.pojo.BusinessUnit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BusinessUnitMapper extends JpaRepository<BusinessUnit, Integer> {
}
